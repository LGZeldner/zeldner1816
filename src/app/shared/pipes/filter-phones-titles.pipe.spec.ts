import { FilterPhonesTitlesPipe } from './filter-phones-titles.pipe';

describe('FilterPhonesTitlesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPhonesTitlesPipe();
    expect(pipe).toBeTruthy();
  });
});
