import { SortPhonesPipe } from './sort-phones.pipe';

describe('SortPhonesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortPhonesPipe();
    expect(pipe).toBeTruthy();
  });
});
