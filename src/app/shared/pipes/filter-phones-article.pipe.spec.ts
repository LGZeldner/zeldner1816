import { FilterPhonesArticlePipe } from './filter-phones-article.pipe';

describe('FilterPhonesArticlePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPhonesArticlePipe();
    expect(pipe).toBeTruthy();
  });
});
